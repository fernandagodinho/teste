# crie uma classe "conta" com os atributos saldo e limite implemente metodos getters e setters para cada um  de atributos, crie um metodo sacar que permite sacar um valor da conta, caso o valor seja maior que o saldo e menor que o saldo + limite, caso contrario imprima uma mensagem de erro. crie um metodo depositar que permite depositar um valor na conta. crie um metodo extrato que imprime o saldo da conta. crie um metodo transferir que transfere um valor para outra conta, caso o valor seja maior que o saldo e menor que o saldo + limite, caso contrario imprima uma mensagem de erro. crie um metodo extrato que imprime o saldo da conta. crie um metodo transferir que transfere um valor para outra conta, caso o valor seja maior que o saldo e menor que o saldo + limite, caso contrario imprima uma mensagem de erro. crie um metodo extrato que imprime o saldo da conta. crie um metodo transferir que transfere um valor para outra conta, caso o valor seja maior que o saldo e menor que o saldo + limite, caso contrario imprima uma mensagem de erro. crie um metodo extrato que imprime o saldo da conta. crie um metodo transferir que transfere um valor para outra conta, caso o valor seja maior que o saldo e menor que o saldo + limite, caso contrario imprima uma mensagem de erro. crie um metodo extrato que imprime o saldo da conta. crie um metodo transferir que transfere um valor para outra conta, caso o valor seja maior que o saldo e menor que o saldo + limite, caso contrario imprima uma mensagem de erro. crie um metodo extrato que imprime o saldo da conta. crie um metodo transferir que transfere um valor para outra conta, caso o valor seja maior que o saldo e menor que o saldo + limite, caso contrario imprima uma mensagem de erro. crie um metodo extrato que imprime o saldo da conta. crie um metodo transferir que transfere um valor para outra conta, caso o valor seja maior que o saldo e menor que o saldo + limite, caso contrario imprima uma mensagem de erro. crie um metodo extrato que imprime o saldo da conta. crie um metodo transferir que transfere um valor para outra conta, caso o valor seja maior que o saldo e menor que o saldo + limite, caso contrario imprima uma mensagem de erro. crie um metodo extrato que imprime o saldo da conta. crie um metodo transferir que

class conta:
    
    
    def __init__(self, numero, titular, saldo, limite, sacar):
        
        
        
        
        self.numero = 123
        
        self.titular = fernanda
        
        self.saldo = 1000
        
        self.limite = 10000
        
        self.sacar = 500
        
        
        
        #getters e setters
        
    def get_numero(self):
        
        return self.numero
    
    def set_numero(self, numero):
        
        self.numero = numero
        
        
    
    
    
    
    
    
    
    
    
    
        